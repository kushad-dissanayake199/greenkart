spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/productdb
    username: root
    password: root
  sql:
    init:
      mode: always

  application:
    name: product-service

server:
  port: 8083

logging:
  level:
    root: INFO
    org.springframework.r2dbc: DEBUG
