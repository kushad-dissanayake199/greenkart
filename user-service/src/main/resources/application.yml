spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/userdb
    username: root
    password: root
  sql:
    init:
      mode: always

  application:
    name: user-service

server:
  port: 8083

logging:
  level:
    root: INFO
    org.springframework.r2dbc: DEBUG
